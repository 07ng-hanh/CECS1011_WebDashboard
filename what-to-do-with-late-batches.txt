late shipment incur the following behaviors:
- shipment feasibility calculation assumes that the shipment is exported in the same day.
- "late" warning in shipment view.
- "unsafe and 'ship by today'" nudges when adding batches through the warehouse management view.
- TTL check for all produces on export.
- no automatic exports
- does not show up on suggestion page.
<all done>

storage condition tracking TODOs:
- health button -> forward thresholds + tracker name [#xxxx produce-name] to sensor view (popup)
- sensor view (threshold tracking mode): reactive banner (on each update, if all within thresholds: show Good, else show Poor + list of risks)
- static historic view: highlight regions that recorded poor metrics by time, starting from harvest-date

auto-threshold: auto-adjust threshold on batch add/remove and override settings. initial check on settings update.

"security" feature: logging requests by user (not needed)